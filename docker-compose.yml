services:
  postgres:
    image: postgres:16-alpine
    container_name: brier-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: brier
      POSTGRES_PASSWORD: brier
      POSTGRES_DB: brier
    ports:
      - '127.0.0.1:5433:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U brier']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - brier-net

  brier:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    container_name: brier-app
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      - HOST=0.0.0.0
      - PORT=8090
      - FRONTEND_DIR=/app/frontend/dist
      - DATABASE_URL=postgresql://brier:brier@postgres:5432/brier
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - '127.0.0.1:${PORT:-8090}:8090'
    healthcheck:
      test: ['CMD-SHELL', 'curl -sf http://localhost:8090/ || exit 1']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'
    networks:
      - brier-net

volumes:
  pgdata:

networks:
  brier-net:
    driver: bridge
