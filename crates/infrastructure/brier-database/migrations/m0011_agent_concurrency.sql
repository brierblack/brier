-- ============================================================
-- m0011: agents.concurrency（并发数，创建时默认 3，无前端入口）
--
-- 字段当前为存档/展示语义（详情属性只读显示），任务执行侧暂不
-- 读取该值做并发闸门。对新库（init.sql 已含该列与默认值）幂等
-- 空跑，旧库既有行自动回填默认 3。
-- ============================================================

ALTER TABLE agents
    ADD COLUMN IF NOT EXISTS concurrency INTEGER NOT NULL DEFAULT 3;
