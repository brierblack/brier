-- ============================================================
-- m0010: agents.model（显式指定的模型名，NULL=由 runtime 自己决定）
--
-- 创建/更新 Agent 时可从模型目录选一个显式模型；缺省不指定，
-- 任务下发时由厂商 CLI 按其配置自行选型。对新库（init.sql 已含
-- 该列）幂等空跑。
-- ============================================================

ALTER TABLE agents
    ADD COLUMN IF NOT EXISTS model TEXT;
